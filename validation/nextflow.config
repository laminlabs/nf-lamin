plugins {
  id 'nf-lamin'
}

manifest {
  name = 'nf-lamin validation workflow'
  mainScript = 'main.nf'
}

providers {
  github {
    token = System.getenv('GITHUB_TOKEN') ?: System.getenv('GITHUB_PAT') ?: ''
  }
}
